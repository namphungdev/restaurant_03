@model IEnumerable<Restaurant.Models.GopY>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="container-fluid">
    <h1>Index</h1>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NoiDung)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TinhTrang)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MaKhachHangNavigation)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.NoiDung)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TinhTrang)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MaKhachHangNavigation.DiaChi)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.MaGopY">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.MaGopY">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.MaGopY">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    </div>